# Copy this file to config/secrets.env (gitignored) and fill in your key.
# secrets.env is loaded into the environment at startup by nurse.config.load_secrets().
# NEVER put a real key in this .example file — it is committed.
ANTHROPIC_API_KEY=sk-ant-...your-key-here...
